# 角色书

一个纯本地的 Vue 手机端 demo，支持：

- 角色列表
- 搜索
- 新增 / 编辑 / 删除
- 自动保存到手机本地 `localStorage`

## 本地运行

```bash
npm install
npm run dev
```

然后在浏览器里打开 Vite 输出的地址，切到手机尺寸预览即可。

## 说明

这个版本是最小可预览 demo，后续可以继续往下面扩展：

- 导出图片
- 导出 Word
- 更完整的本地数据库
- 打包 Android APK
